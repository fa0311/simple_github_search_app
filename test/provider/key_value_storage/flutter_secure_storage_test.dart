import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Github の Token設定', (tester) async {
    // final container = createContainer();
    // await container.read(githubTokenSettingProvider.notifier).set('Test');
    // final data = container.read(githubTokenSettingProvider);
    // await container.read(githubTokenSettingProvider.notifier).remove();
    // expect(data, 'Test');
  });
}
