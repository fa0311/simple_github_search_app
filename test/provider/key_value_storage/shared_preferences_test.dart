import 'package:flutter_test/flutter_test.dart';

void main() {
  test('テーマ設定', () async {
    // final container = createContainer();
    // await container.read(themeSettingProvider.notifier).set(ThemeMode.dark);
    // final data = container.read(themeSettingProvider);
    // expect(data, ThemeMode.dark);
  });

  test('言語設定', () async {
    // final container = createContainer();

    // await container.read(languageSettingProvider.notifier).set(const Locale('ja', 'JP'));
    // final data = container.read(languageSettingProvider);

    // container.read(languageSettingProvider.notifier).defaultLocale();

    // expect(data, const Locale('ja', 'JP'));
  });
}
