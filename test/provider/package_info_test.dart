import 'package:flutter_test/flutter_test.dart';

void main() {
  test('テーマ設定', () async {
    // final container = ProviderContainer();

    // final res = await container.read(getPackageInfoProvider.future);
    // expect(res, isA<PackageInfo>());
  });
}
